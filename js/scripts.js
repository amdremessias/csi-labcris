// scripts.js
document.addEventListener('DOMContentLoaded', () => {
  // Widget Container Content
  const widgetContainer = document.getElementById('widget-container');
  widgetContainer.innerHTML = '<p>Bem-vindo ao widget CSI2! Aqui você encontra soluções de segurança.</p>';

  // Console log for debugging
  console.log('Site CSI2 está funcionando perfeitamente!');
});
